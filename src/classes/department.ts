// Importing Vehicle and Wheel classes


// department class 
class Department {
  // Declare properties of the department class
  name: string;
  // Constructor for the department class
  constructor(
    name:string,
  ) {
    // Initialize properties of the department class
    this.name = name;
  }
}

// Export the department class as the default export
export default Department;
